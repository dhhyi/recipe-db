query AllRecipes {
  recipes {
    id
    name
  }
}

query AllRecipesFull {
  recipes {
    id
    name
    rating {
      average
      count
    }
  }
}

query RecipeById($id: ID!) {
  recipe(id: $id) {
    id
    name
    method
    ingredients {
      name
      amount
      unit
      optional
    }
    inspirations {
      url
      extracted {
        title
        description
        canonical
        inlinedFavicon
      }
    }
  }
}

query RatingByRecipeId($id: ID!) {
  rating(id: $id) {
    average
    count
  }
}

query RecipesEditOnline {
  isServiceOnline(name: "recipes-edit")
}
