query AllRecipes {
  recipes {
    id
    name
  }
}

query RecipeById($id: ID!) {
  recipe(id: $id) {
    id
    name
  }
}

query RatingByRecipeId($id: ID!) {
  rating(id: $id) {
    average
    count
  }
}
