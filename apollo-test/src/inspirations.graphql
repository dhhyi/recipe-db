query RecipeByIdWithInspirationQuery($id: ID!) {
  recipe(id: $id) {
    name
    inspirations {
      url
      extracted {
        title
        description
        canonical
        favicon
      }
    }
  }
}
