query ($id: ID!) {
  recipe(id: $id) {
    name
    inspirations {
      url
      extracted {
        canonical
        description
        favicon
        inlinedFavicon
        title
      }
    }
  }
}
