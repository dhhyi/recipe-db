{
  "name": "recipe-db",
  "scripts": {
    "prepare": "husky install",
    "synchronize": "node synchronize.js",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "husky": "*",
    "js-yaml": "^4.1.0",
    "lint-staged": "*",
    "prettier": "^2.0.0"
  },
  "license": "MIT",
  "lint-staged": {
    "*": [
      "prettier -u --write",
      "npm run synchronize"
    ]
  }
}
