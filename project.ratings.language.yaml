extends: dcc://javascript
devcontainer:
  build:
    root:
      - RUN npm i -g json-server
  ports:
    - 3000
  initialize: json-server --host 0.0.0.0 -w db.js
extras:
  - traefik
traefik:
  network: intranet
  labels:
    http:
      routers:
        ratings:
          rule: PathPrefix(`/ratings`)
          entrypoints: rest-internal
